name: Release on Main

on:
  push:
    branches:
      - main

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      project_a: ${{ steps.filter.outputs.project_a }}
      project_b: ${{ steps.filter.outputs.project_b }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            project_a:
              - 'project-a/**'
            project_b:
              - 'project-b/**'

  release-project-a:
    needs: detect-changes
    if: needs.detect-changes.outputs.project_a == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 发布 project-a
        id: release
        uses: ./.github/actions/release-project
        with:
          project_dir: project-a
          tag_prefix: a

      - name: 推送 tag 和版本提交
        if: steps.release.outputs.released == 'true'
        run: git push origin main --tags

      - name: 登录 Container Registry
        if: steps.release.outputs.released == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并推送 project-a Docker 镜像
        if: steps.release.outputs.released == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ./project-a
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}/project-a:${{ steps.release.outputs.tag }}
            ${{ env.REGISTRY }}/${{ github.repository }}/project-a:latest

  release-project-b:
    needs: detect-changes
    if: needs.detect-changes.outputs.project_b == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 发布 project-b
        id: release
        uses: ./.github/actions/release-project
        with:
          project_dir: project-b
          tag_prefix: b

      - name: 推送 tag 和版本提交
        if: steps.release.outputs.released == 'true'
        run: git push origin main --tags

      - name: 登录 Container Registry
        if: steps.release.outputs.released == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并推送 project-b Docker 镜像
        if: steps.release.outputs.released == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ./project-b
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}/project-b:${{ steps.release.outputs.tag }}
            ${{ env.REGISTRY }}/${{ github.repository }}/project-b:latest
